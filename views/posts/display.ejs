<!DOCTYPE html>
<html>
<head>
  <title><%= posts.name %></title>
  <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css" />
  <link rel="stylesheet" href="/css/general.css" type="text/css" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <div class="col-sm-12">
        <h1><%= posts.name %></h1>
        <p class="byline">By <%= posts.author %> <%= posts.date %></p>
      </div>
    </div>
    <div class="row">
      <div class="col-sm-12">
        <% if (posts.image){ %>
        <img id="thumbnail-image" src="<%= posts.image %>" alt="thumbnail image">
        <% } %>
        <pre><%= posts.content %></pre>
        <a href="/posts/edit/<%= posts.link %>">Edit post</a>
      </div>
    </div>
</div>
</body>
</html>
